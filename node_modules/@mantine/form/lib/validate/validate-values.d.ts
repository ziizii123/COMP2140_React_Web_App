import { FormValidateInput, FormValidationResult } from '../types';
export declare const formRootRule: unique symbol;
export declare function validateValues<T>(validate: FormValidateInput<T> | undefined, values: T, resolveValidationError?: (error: unknown) => React.ReactNode, signal?: AbortSignal): FormValidationResult | Promise<FormValidationResult>;
