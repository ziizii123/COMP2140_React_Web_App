"use client";
const require_is_element = require("../../core/utils/is-element/is-element.cjs");
const require_get_size = require("../../core/utils/get-size/get-size.cjs");
const require_create_vars_resolver = require("../../core/styles-api/create-vars-resolver/create-vars-resolver.cjs");
const require_use_props = require("../../core/MantineProvider/use-props/use-props.cjs");
const require_use_styles = require("../../core/styles-api/use-styles/use-styles.cjs");
const require_factory = require("../../core/factory/factory.cjs");
const require_Box = require("../../core/Box/Box.cjs");
const require_Breadcrumbs_module = require("./Breadcrumbs.module.cjs");
let react = require("react");
let react_jsx_runtime = require("react/jsx-runtime");
//#region packages/@mantine/core/src/components/Breadcrumbs/Breadcrumbs.tsx
const defaultProps = { separator: "/" };
const varsResolver = require_create_vars_resolver.createVarsResolver((_, { separatorMargin }) => ({ root: { "--bc-separator-margin": require_get_size.getSpacing(separatorMargin) } }));
const Breadcrumbs = require_factory.factory((_props) => {
	const props = require_use_props.useProps("Breadcrumbs", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, children, separator, separatorMargin, attributes, ...others } = props;
	const getStyles = require_use_styles.useStyles({
		name: "Breadcrumbs",
		classes: require_Breadcrumbs_module.default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	const items = react.Children.toArray(children).reduce((acc, child, index, array) => {
		const item = require_is_element.isElement(child) ? (0, react.cloneElement)(child, {
			...getStyles("breadcrumb", { className: child.props?.className }),
			key: index
		}) : /* @__PURE__ */ (0, react.createElement)("div", {
			...getStyles("breadcrumb"),
			key: index
		}, child);
		acc.push(item);
		if (index !== array.length - 1) acc.push(/* @__PURE__ */ (0, react.createElement)(require_Box.Box, {
			...getStyles("separator"),
			key: `separator-${index}`
		}, separator));
		return acc;
	}, []);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_Box.Box, {
		...getStyles("root"),
		...others,
		children: items
	});
});
Breadcrumbs.classes = require_Breadcrumbs_module.default;
Breadcrumbs.varsResolver = varsResolver;
Breadcrumbs.displayName = "@mantine/core/Breadcrumbs";
//#endregion
exports.Breadcrumbs = Breadcrumbs;

//# sourceMappingURL=Breadcrumbs.cjs.map