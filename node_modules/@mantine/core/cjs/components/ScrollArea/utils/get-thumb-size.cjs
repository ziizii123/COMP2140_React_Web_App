"use client";
const require_get_thumb_ratio = require("./get-thumb-ratio.cjs");
//#region packages/@mantine/core/src/components/ScrollArea/utils/get-thumb-size.tsx
function getThumbSize(sizes) {
	const ratio = require_get_thumb_ratio.getThumbRatio(sizes.viewport, sizes.content);
	const scrollbarPadding = sizes.scrollbar.paddingStart + sizes.scrollbar.paddingEnd;
	const thumbSize = (sizes.scrollbar.size - scrollbarPadding) * ratio;
	return Math.max(thumbSize, 18);
}
//#endregion
exports.getThumbSize = getThumbSize;

//# sourceMappingURL=get-thumb-size.cjs.map