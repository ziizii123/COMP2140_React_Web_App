"use client";
const require_get_default_z_index = require("../../core/utils/get-default-z-index/get-default-z-index.cjs");
const require_use_props = require("../../core/MantineProvider/use-props/use-props.cjs");
const require_use_styles = require("../../core/styles-api/use-styles/use-styles.cjs");
const require_factory = require("../../core/factory/factory.cjs");
const require_Paper = require("../Paper/Paper.cjs");
const require_Transition = require("../Transition/Transition.cjs");
const require_Affix = require("../Affix/Affix.cjs");
const require_ActionBar_context = require("./ActionBar.context.cjs");
const require_ActionBar_module = require("./ActionBar.module.cjs");
const require_ActionBarCloseButton = require("./ActionBarCloseButton/ActionBarCloseButton.cjs");
const require_ActionBarDivider = require("./ActionBarDivider/ActionBarDivider.cjs");
let react_remove_scroll = require("react-remove-scroll");
let react = require("react");
let react_jsx_runtime = require("react/jsx-runtime");
//#region packages/@mantine/core/src/components/ActionBar/ActionBar.tsx
const defaultProps = {
	withBorder: true,
	py: "xs",
	px: "sm",
	position: {
		bottom: 30,
		left: 0,
		right: 0
	},
	transitionProps: {
		transition: "pop",
		duration: 200
	},
	closeOnEscape: false,
	withinPortal: true,
	"aria-label": "Actions",
	zIndex: require_get_default_z_index.getDefaultZIndex("modal")
};
const ActionBar = require_factory.factory((_props) => {
	const props = require_use_props.useProps("ActionBar", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, zIndex, position, keepMounted, opened, transitionProps, withinPortal, children, onClose, portalProps, closeOnEscape, attributes, ...others } = props;
	const getStyles = require_use_styles.useStyles({
		name: "ActionBar",
		classes: require_ActionBar_module.default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars
	});
	const handleEscape = (0, react.useEffectEvent)((event) => {
		if (event.key === "Escape" && !event.isComposing) onClose?.();
	});
	(0, react.useEffect)(() => {
		if (!closeOnEscape || !opened) return;
		window.addEventListener("keydown", handleEscape, { passive: true });
		return () => window.removeEventListener("keydown", handleEscape);
	}, [closeOnEscape, opened]);
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_Affix.Affix, {
		zIndex,
		position,
		withinPortal,
		portalProps,
		unstyled,
		className: react_remove_scroll.RemoveScroll.classNames.fullWidth,
		children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_Transition.Transition, {
			keepMounted,
			keepMountedMode: "display-none",
			mounted: opened,
			...transitionProps,
			children: (transitionStyles) => /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_ActionBar_context.ActionBarProvider, {
				value: {
					onClose,
					getStyles,
					unstyled
				},
				children: /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_Paper.Paper, {
					unstyled,
					...getStyles("root", { style: transitionStyles }),
					role: "group",
					...others,
					children
				})
			})
		})
	});
});
ActionBar.classes = require_ActionBar_module.default;
ActionBar.displayName = "@mantine/core/ActionBar";
ActionBar.Divider = require_ActionBarDivider.ActionBarDivider;
ActionBar.CloseButton = require_ActionBarCloseButton.ActionBarCloseButton;
//#endregion
exports.ActionBar = ActionBar;

//# sourceMappingURL=ActionBar.cjs.map