"use client";
const require_create_vars_resolver = require("../../core/styles-api/create-vars-resolver/create-vars-resolver.cjs");
const require_use_props = require("../../core/MantineProvider/use-props/use-props.cjs");
const require_use_styles = require("../../core/styles-api/use-styles/use-styles.cjs");
const require_factory = require("../../core/factory/factory.cjs");
const require_Box = require("../../core/Box/Box.cjs");
const require_get_mark_color = require("./get-mark-color.cjs");
const require_Mark_module = require("./Mark.module.cjs");
let react_jsx_runtime = require("react/jsx-runtime");
//#region packages/@mantine/core/src/components/Mark/Mark.tsx
const defaultProps = { color: "yellow" };
const varsResolver = require_create_vars_resolver.createVarsResolver((theme, { color }) => ({ root: {
	"--mark-bg-dark": require_get_mark_color.getMarkColor({
		color,
		theme,
		defaultShade: 5
	}),
	"--mark-bg-light": require_get_mark_color.getMarkColor({
		color,
		theme,
		defaultShade: 2
	})
} }));
const Mark = require_factory.factory((_props) => {
	const props = require_use_props.useProps("Mark", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, color, variant, attributes, ...others } = props;
	const getStyles = require_use_styles.useStyles({
		name: "Mark",
		props,
		className,
		style,
		classes: require_Mark_module.default,
		classNames,
		styles,
		unstyled,
		attributes,
		vars,
		varsResolver
	});
	return /* @__PURE__ */ (0, react_jsx_runtime.jsx)(require_Box.Box, {
		component: "mark",
		variant,
		...getStyles("root"),
		...others
	});
});
Mark.classes = require_Mark_module.default;
Mark.varsResolver = varsResolver;
Mark.displayName = "@mantine/core/Mark";
//#endregion
exports.Mark = Mark;

//# sourceMappingURL=Mark.cjs.map