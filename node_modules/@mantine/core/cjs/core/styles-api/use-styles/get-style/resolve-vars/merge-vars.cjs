"use client";
const require_filter_props = require("../../../../utils/filter-props/filter-props.cjs");
//#region packages/@mantine/core/src/core/styles-api/use-styles/get-style/resolve-vars/merge-vars.ts
function mergeVars(vars) {
	return vars.reduce((acc, current) => {
		if (current) Object.keys(current).forEach((key) => {
			acc[key] = {
				...acc[key],
				...require_filter_props.filterProps(current[key])
			};
		});
		return acc;
	}, {});
}
//#endregion
exports.mergeVars = mergeVars;

//# sourceMappingURL=merge-vars.cjs.map