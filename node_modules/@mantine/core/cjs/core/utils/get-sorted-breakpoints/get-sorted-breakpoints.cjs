"use client";
const require_get_breakpoint_value = require("../get-breakpoint-value/get-breakpoint-value.cjs");
//#region packages/@mantine/core/src/core/utils/get-sorted-breakpoints/get-sorted-breakpoints.ts
function getSortedBreakpoints(values, breakpoints) {
	const convertedBreakpoints = values.map((breakpoint) => ({
		value: breakpoint,
		px: require_get_breakpoint_value.getBreakpointValue(breakpoint, breakpoints)
	}));
	convertedBreakpoints.sort((a, b) => a.px - b.px);
	return convertedBreakpoints;
}
//#endregion
exports.getSortedBreakpoints = getSortedBreakpoints;

//# sourceMappingURL=get-sorted-breakpoints.cjs.map