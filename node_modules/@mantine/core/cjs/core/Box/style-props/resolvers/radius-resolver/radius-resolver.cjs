"use client";
const require_rem = require("../../../../utils/units-converters/rem.cjs");
//#region packages/@mantine/core/src/core/Box/style-props/resolvers/radius-resolver/radius-resolver.ts
function radiusResolver(value, theme) {
	if (typeof value === "string" && value in theme.radius) return `var(--mantine-radius-${value})`;
	if (typeof value === "number") return require_rem.rem(value);
	if (typeof value === "string") return require_rem.rem(value);
	return value;
}
//#endregion
exports.radiusResolver = radiusResolver;

//# sourceMappingURL=radius-resolver.cjs.map