"use client";
const require_rem = require("../../../../utils/units-converters/rem.cjs");
const require_color_resolver = require("../color-resolver/color-resolver.cjs");
//#region packages/@mantine/core/src/core/Box/style-props/resolvers/border-resolver/border-resolver.ts
function borderResolver(value, theme) {
	if (typeof value === "number") return require_rem.rem(value);
	if (typeof value === "string") {
		const [size, style, ...colorTuple] = value.split(" ").filter((val) => val.trim() !== "");
		let result = `${require_rem.rem(size)}`;
		style && (result += ` ${style}`);
		colorTuple.length > 0 && (result += ` ${require_color_resolver.colorResolver(colorTuple.join(" "), theme)}`);
		return result.trim();
	}
	return value;
}
//#endregion
exports.borderResolver = borderResolver;

//# sourceMappingURL=border-resolver.cjs.map