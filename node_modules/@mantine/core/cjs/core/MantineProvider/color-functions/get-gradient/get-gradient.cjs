"use client";
const require_get_theme_color = require("../get-theme-color/get-theme-color.cjs");
//#region packages/@mantine/core/src/core/MantineProvider/color-functions/get-gradient/get-gradient.ts
function getGradient(gradient, theme) {
	const merged = {
		from: gradient?.from || theme.defaultGradient.from,
		to: gradient?.to || theme.defaultGradient.to,
		deg: gradient?.deg ?? theme.defaultGradient.deg ?? 0
	};
	const fromColor = require_get_theme_color.getThemeColor(merged.from, theme);
	const toColor = require_get_theme_color.getThemeColor(merged.to, theme);
	return `linear-gradient(${merged.deg}deg, ${fromColor} 0%, ${toColor} 100%)`;
}
//#endregion
exports.getGradient = getGradient;

//# sourceMappingURL=get-gradient.cjs.map