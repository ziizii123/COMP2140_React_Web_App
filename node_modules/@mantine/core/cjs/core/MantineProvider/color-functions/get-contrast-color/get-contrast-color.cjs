"use client";
const require_get_primary_shade = require("../get-primary-shade/get-primary-shade.cjs");
const require_parse_theme_color = require("../parse-theme-color/parse-theme-color.cjs");
const require_virtual_color = require("../../MantineCssVariables/virtual-color/virtual-color.cjs");
//#region packages/@mantine/core/src/core/MantineProvider/color-functions/get-contrast-color/get-contrast-color.ts
function getContrastColor({ color, theme, autoContrast, colorScheme }) {
	if (!(typeof autoContrast === "boolean" ? autoContrast : theme.autoContrast)) return "var(--mantine-color-white)";
	return require_parse_theme_color.parseThemeColor({
		color: color || theme.primaryColor,
		theme,
		colorScheme
	}).isLight ? "var(--mantine-color-black)" : "var(--mantine-color-white)";
}
function getVirtualColorContrast(value, theme, colorScheme) {
	return getContrastColor({
		color: colorScheme === "dark" ? value.dark : value.light,
		theme,
		colorScheme,
		autoContrast: true
	});
}
function getPrimaryContrastColor(theme, colorScheme) {
	const primaryColor = theme.colors[theme.primaryColor];
	if (require_virtual_color.isVirtualColor(primaryColor)) {
		if (!theme.autoContrast) return "var(--mantine-color-white)";
		return getVirtualColorContrast(primaryColor, theme, colorScheme);
	}
	return getContrastColor({
		color: primaryColor[require_get_primary_shade.getPrimaryShade(theme, colorScheme)],
		theme,
		autoContrast: null
	});
}
//#endregion
exports.getContrastColor = getContrastColor;
exports.getPrimaryContrastColor = getPrimaryContrastColor;
exports.getVirtualColorContrast = getVirtualColorContrast;

//# sourceMappingURL=get-contrast-color.cjs.map