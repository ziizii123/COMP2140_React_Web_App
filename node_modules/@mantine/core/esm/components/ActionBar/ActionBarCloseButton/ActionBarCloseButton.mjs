"use client";
import { useProps } from "../../../core/MantineProvider/use-props/use-props.mjs";
import { factory } from "../../../core/factory/factory.mjs";
import { CloseButton } from "../../CloseButton/CloseButton.mjs";
import { useActionBarContext } from "../ActionBar.context.mjs";
import ActionBar_module_default from "../ActionBar.module.mjs";
import { jsx } from "react/jsx-runtime";
//#region packages/@mantine/core/src/components/ActionBar/ActionBarCloseButton/ActionBarCloseButton.tsx
const defaultProps = {};
const ActionBarCloseButton = factory((_props) => {
	const { classNames, className, style, styles, unstyled, onClick, ...others } = useProps("ActionBarCloseButton", defaultProps, _props);
	const ctx = useActionBarContext();
	return /* @__PURE__ */ jsx(CloseButton, {
		...ctx.getStyles("closeButton", {
			className,
			style,
			classNames,
			styles
		}),
		unstyled: unstyled ?? ctx.unstyled,
		...others,
		onClick: (event) => {
			onClick?.(event);
			ctx.onClose?.();
		}
	});
});
ActionBarCloseButton.classes = ActionBar_module_default;
ActionBarCloseButton.displayName = "@mantine/core/ActionBarCloseButton";
//#endregion
export { ActionBarCloseButton };

//# sourceMappingURL=ActionBarCloseButton.mjs.map