"use client";
import { getDefaultZIndex } from "../../core/utils/get-default-z-index/get-default-z-index.mjs";
import { useProps } from "../../core/MantineProvider/use-props/use-props.mjs";
import { useStyles } from "../../core/styles-api/use-styles/use-styles.mjs";
import { factory } from "../../core/factory/factory.mjs";
import { Paper } from "../Paper/Paper.mjs";
import { Transition } from "../Transition/Transition.mjs";
import { Affix } from "../Affix/Affix.mjs";
import { ActionBarProvider } from "./ActionBar.context.mjs";
import ActionBar_module_default from "./ActionBar.module.mjs";
import { ActionBarCloseButton } from "./ActionBarCloseButton/ActionBarCloseButton.mjs";
import { ActionBarDivider } from "./ActionBarDivider/ActionBarDivider.mjs";
import { RemoveScroll } from "react-remove-scroll";
import { useEffect, useEffectEvent } from "react";
import { jsx } from "react/jsx-runtime";
//#region packages/@mantine/core/src/components/ActionBar/ActionBar.tsx
const defaultProps = {
	withBorder: true,
	py: "xs",
	px: "sm",
	position: {
		bottom: 30,
		left: 0,
		right: 0
	},
	transitionProps: {
		transition: "pop",
		duration: 200
	},
	closeOnEscape: false,
	withinPortal: true,
	"aria-label": "Actions",
	zIndex: getDefaultZIndex("modal")
};
const ActionBar = factory((_props) => {
	const props = useProps("ActionBar", defaultProps, _props);
	const { classNames, className, style, styles, unstyled, vars, zIndex, position, keepMounted, opened, transitionProps, withinPortal, children, onClose, portalProps, closeOnEscape, attributes, ...others } = props;
	const getStyles = useStyles({
		name: "ActionBar",
		classes: ActionBar_module_default,
		props,
		className,
		style,
		classNames,
		styles,
		unstyled,
		attributes,
		vars
	});
	const handleEscape = useEffectEvent((event) => {
		if (event.key === "Escape" && !event.isComposing) onClose?.();
	});
	useEffect(() => {
		if (!closeOnEscape || !opened) return;
		window.addEventListener("keydown", handleEscape, { passive: true });
		return () => window.removeEventListener("keydown", handleEscape);
	}, [closeOnEscape, opened]);
	return /* @__PURE__ */ jsx(Affix, {
		zIndex,
		position,
		withinPortal,
		portalProps,
		unstyled,
		className: RemoveScroll.classNames.fullWidth,
		children: /* @__PURE__ */ jsx(Transition, {
			keepMounted,
			keepMountedMode: "display-none",
			mounted: opened,
			...transitionProps,
			children: (transitionStyles) => /* @__PURE__ */ jsx(ActionBarProvider, {
				value: {
					onClose,
					getStyles,
					unstyled
				},
				children: /* @__PURE__ */ jsx(Paper, {
					unstyled,
					...getStyles("root", { style: transitionStyles }),
					role: "group",
					...others,
					children
				})
			})
		})
	});
});
ActionBar.classes = ActionBar_module_default;
ActionBar.displayName = "@mantine/core/ActionBar";
ActionBar.Divider = ActionBarDivider;
ActionBar.CloseButton = ActionBarCloseButton;
//#endregion
export { ActionBar };

//# sourceMappingURL=ActionBar.mjs.map