import { BoxProps, ElementProps, Factory, MantineRadius, MantineShadow, StylesApiProps } from '../../core';
import { AffixBaseProps } from '../Affix';
import { PaperBaseProps } from '../Paper';
import { TransitionOverride } from '../Transition';
import { ActionBarCloseButton, ActionBarCloseButtonStylesNames } from './ActionBarCloseButton/ActionBarCloseButton';
import { ActionBarDivider, ActionBarDividerStylesNames } from './ActionBarDivider/ActionBarDivider';
export type ActionBarStylesNames = 'root' | ActionBarDividerStylesNames | ActionBarCloseButtonStylesNames;
export interface ActionBarProps extends BoxProps, AffixBaseProps, PaperBaseProps, StylesApiProps<ActionBarFactory>, ElementProps<'div'> {
    /** Controls visibility */
    opened: boolean;
    /** Called when close button is clicked or Escape is pressed */
    onClose?: () => void;
    /** Props passed down to the `Transition` component, `{ transition: 'pop', duration: 200 }` by default */
    transitionProps?: TransitionOverride;
    /** Key of `theme.shadows` or any valid CSS `box-shadow` value */
    shadow?: MantineShadow;
    /** Key of `theme.radius` or any valid CSS value to set `border-radius` */
    radius?: MantineRadius;
    /** Determines whether the action bar should be closed when `Escape` key is pressed, `false` by default */
    closeOnEscape?: boolean;
    /** `aria-label` of the actions group, `'Actions'` by default */
    'aria-label'?: string;
    /** If set, the component uses `display: none` to hide the root element instead of removing the DOM node, `false` by default */
    keepMounted?: boolean;
    children: React.ReactNode;
}
export type ActionBarFactory = Factory<{
    props: ActionBarProps;
    ref: HTMLDivElement;
    stylesNames: ActionBarStylesNames;
    staticComponents: {
        Divider: typeof ActionBarDivider;
        CloseButton: typeof ActionBarCloseButton;
    };
}>;
export declare const ActionBar: import("../..").MantineComponent<{
    props: ActionBarProps;
    ref: HTMLDivElement;
    stylesNames: ActionBarStylesNames;
    staticComponents: {
        Divider: typeof ActionBarDivider;
        CloseButton: typeof ActionBarCloseButton;
    };
}>;
