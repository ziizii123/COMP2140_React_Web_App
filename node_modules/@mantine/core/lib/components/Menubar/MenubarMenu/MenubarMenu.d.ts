import { MenuProps } from '../../Menu';
export interface MenubarMenuProps extends Omit<MenuProps, 'opened' | 'defaultOpened' | 'onChange' | 'onOpen' | 'onClose' | 'trigger' | 'returnFocus'> {
    /** `Menubar.Target` and `Menubar.Dropdown` components */
    children?: React.ReactNode;
}
export declare function MenubarMenu({ children, transitionProps, ...others }: MenubarMenuProps): import("react").JSX.Element;
export declare namespace MenubarMenu {
    var displayName: string;
}
