import { Factory } from '../../../core';
import { ColorSliderOptions, ColorSliderStylesNames } from '../ColorSlider/ColorSlider';
export interface AlphaSliderProps extends ColorSliderOptions {
    color: string;
}
export type AlphaSliderFactory = Factory<{
    props: AlphaSliderProps;
    ref: HTMLDivElement;
    stylesNames: ColorSliderStylesNames;
}>;
export declare const AlphaSlider: import("../../..").MantineComponent<{
    props: AlphaSliderProps;
    ref: HTMLDivElement;
    stylesNames: ColorSliderStylesNames;
}>;
export declare namespace AlphaSlider {
    type Props = AlphaSliderProps;
    type Factory = AlphaSliderFactory;
}
