import { ElementProps } from '../../../core';
export interface SwatchesProps extends ElementProps<'div'> {
    size?: string | number;
    data: string[];
    swatchesPerRow?: number;
    focusable?: boolean;
    value?: string;
    onChangeEnd?: (color: string) => void;
    setValue: (value: string) => void;
}
export declare function Swatches({ className, datatype, setValue, onChangeEnd, size, focusable, data, swatchesPerRow, value, ...others }: SwatchesProps): import("react").JSX.Element;
export declare namespace Swatches {
    var displayName: string;
}
