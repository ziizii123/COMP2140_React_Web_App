import { Factory } from '../../../core';
import { ColorSliderOptions, ColorSliderStylesNames } from '../ColorSlider/ColorSlider';
export interface HueSliderProps extends ColorSliderOptions {
}
export type HueSliderFactory = Factory<{
    props: HueSliderProps;
    ref: HTMLDivElement;
    stylesNames: ColorSliderStylesNames;
}>;
export declare const HueSlider: import("../../..").MantineComponent<{
    props: HueSliderProps;
    ref: HTMLDivElement;
    stylesNames: ColorSliderStylesNames;
}>;
export declare namespace HueSlider {
    type Props = HueSliderProps;
    type Factory = HueSliderFactory;
}
