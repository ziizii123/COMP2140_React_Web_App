import { Primitive } from '../../../core';
import { ComboboxItem, ComboboxParsedItem } from '../Combobox.types';
export declare function getOptionsLockup<Value extends Primitive = string>(options: ComboboxParsedItem<Value>[]): Record<PropertyKey, ComboboxItem<Value>>;
