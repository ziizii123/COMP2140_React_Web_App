import { Primitive } from '../../../core';
import { ComboboxParsedItem } from '../Combobox.types';
export interface FilterOptionsInput<Value extends Primitive = Primitive> {
    options: ComboboxParsedItem<Value>[];
    search: string;
    limit: number;
}
export declare function defaultOptionsFilter({ options, search, limit, }: FilterOptionsInput): ComboboxParsedItem<Primitive>[];
