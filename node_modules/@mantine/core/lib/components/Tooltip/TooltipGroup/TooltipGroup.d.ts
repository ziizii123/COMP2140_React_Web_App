import { ExtendComponent, Factory, MantineThemeComponent } from '../../../core';
export interface TooltipGroupContextValue {
    withinGroup: boolean;
}
export declare const TooltipGroupContext: import("react").Context<TooltipGroupContextValue>;
export interface TooltipGroupProps {
    /** `Tooltip` components */
    children: React.ReactNode;
    /** Open delay in ms */
    openDelay?: number;
    /** Close delay in ms */
    closeDelay?: number;
}
export declare function TooltipGroup(props: TooltipGroupProps): import("react").JSX.Element;
export declare namespace TooltipGroup {
    var displayName: string;
    var extend: (c: ExtendComponent<TooltipGroupFactory>) => MantineThemeComponent;
}
export type TooltipGroupFactory = Factory<{
    props: TooltipGroupProps;
}>;
