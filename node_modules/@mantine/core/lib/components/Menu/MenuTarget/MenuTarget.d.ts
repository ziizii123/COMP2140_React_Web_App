export interface MenuTargetProps {
    /** Target element */
    children: React.ReactNode;
    /** Key of the prop used to get element ref, useful for forwarding refs to custom components @default 'ref' */
    refProp?: string;
}
export declare function MenuTarget(props: MenuTargetProps): import("react").JSX.Element;
export declare namespace MenuTarget {
    var displayName: string;
}
