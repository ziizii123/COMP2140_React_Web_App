import type { MantineColorScheme } from '../theme.types';
export interface ColorSchemeScriptProps extends React.ComponentProps<'script'> {
    forceColorScheme?: 'light' | 'dark';
    defaultColorScheme?: MantineColorScheme;
    localStorageKey?: string;
}
export declare function ColorSchemeScript({ defaultColorScheme, localStorageKey, forceColorScheme, ...others }: ColorSchemeScriptProps): import("react").JSX.Element;
